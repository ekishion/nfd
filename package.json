{
  "name": "nfd",
  "version": "1.0.0",
  "description": "No Fraud / Node Forward Bot - Cloudflare Worker Telegram Bot",
  "main": "worker.js",
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && node -c worker.js && node --input-type=module -e \"import('./worker.js')\" && node build.js --all && node -c worker.js && node --input-type=module -e \"import('./worker.js')\" && node build.js",
    "test:full": "npm test && node test/index.js"
  },
  "keywords": [
    "telegram",
    "bot",
    "cloudflare-worker",
    "forwarder"
  ],
  "author": "",
  "license": "GPL-3.0"
}
